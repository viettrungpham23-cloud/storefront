# ── Bí mật & dữ liệu cá nhân (KHÔNG commit) ───────────────────────
.env
.env.*
secrets/
**/*.key
*.keystore
google-services.json

# Cơ sở dữ liệu (dữ liệu sinh ra / có PII)
*.db
*.db.bak
*.db-journal
*.sqlite
*.sqlite3

# Hồ sơ khách (CCCD, ảnh giấy tờ — NHẠY CẢM)
QLBH-Website/customer_db/

# SQL dumps (có thể chứa PII)
old_db_dump.sql

# ── Sinh tự động / build ──────────────────────────────────────────
__pycache__/
*.pyc
node_modules/
dist/
build/
.vite/

# Bản web đã đóng gói + build native (sinh lại từ web/)
mobile/www/
mobile/android/app/build/
mobile/android/.gradle/
mobile/ios/App/build/
mobile/ios/App/Pods/

# Tài liệu/artefact thử nghiệm không thuộc runtime
looper-*/
mobile/Reference/

# File đóng gói / binary lớn — không thuộc source code
*.apk
*.aab
*.ipa
*.tar.gz
deploy.tar.gz
qlbh-deploy.tar.gz

# File tạm
temp_append.js

# ── Hệ điều hành / công cụ ────────────────────────────────────────
.DS_Store
*.log
.impeccable/
venv/
.venv/
.claude/
